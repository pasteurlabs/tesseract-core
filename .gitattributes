# Force LF on every text file. Shell scripts are COPY'd into Linux containers
# during `tesseract build` and executed with bash, which fails on CRLF
# (e.g. `$'\r': command not found`) when checked out on Windows with
# core.autocrlf=true.
* text=auto eol=lf
