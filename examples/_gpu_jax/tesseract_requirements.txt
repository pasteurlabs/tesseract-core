# JAX provides the device arrays returned by apply(). The [cuda12] variant
# bundles its own CUDA pip wheels, so it runs on the GPU even though the runtime
# base image ships only the CUDA runtime (the [cuda12-local] variant links
# against a full in-image CUDA toolkit that isn't present and silently falls back
# to CPU). JAX exposes device buffers only via DLPack, which is the path this
# example exercises. The GPU CI matrix swaps in tesseract_requirements_cuda13.txt
# for the 13.x leg.
jax[cuda12]
