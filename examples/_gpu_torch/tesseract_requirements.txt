# PyTorch provides the device arrays returned by apply(). The cu126 wheel index
# gives a CUDA 12 build matching the 12.x runtime base_image; the wheel bundles
# its own CUDA libraries. Torch tensors expose __cuda_array_interface__, the CAI
# path of the CUDA IPC export.
--extra-index-url https://download.pytorch.org/whl/cu126
torch
