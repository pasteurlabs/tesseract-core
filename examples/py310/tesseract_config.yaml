name: "py310"
version: "0.1.0"
description: |
  Empty Tesseract that requires Python 3.10 (set through
  build_config.requirements.python_version).

build_config:
  requirements:
    provider: uv-pip
    python_version: "3.10"
