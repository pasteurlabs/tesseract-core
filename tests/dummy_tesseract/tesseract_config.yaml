name: unit_vectoradd
version: "1.2.3"
description: |
  Simple tesseract that adds two vectors.

metadata:
  tags:
    - ml
    - physics
  nested:
    key: value

build_config:
  package_data: []
  custom_build_steps: []
