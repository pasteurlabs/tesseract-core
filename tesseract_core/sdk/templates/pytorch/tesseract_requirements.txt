# Tesseract requirements file
# Generated by tesseract {{version}} on {{timestamp}}

# Add Python requirements like this:
# `--extra-index-url` supplements PyPI (rather than replacing it, as `--index-url`
# would), so packages not hosted on PyTorch's index -- including build backends
# for local path dependencies -- still resolve from PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# If a package name is served by more than one index, uv's default strategy picks
# the first index it appears on, which may not be the one you want.

# This may contain private dependencies via SSH URLs:
# git+ssh://git@github.com/username/repo.git@branch
# (use `tesseract build --forward-ssh-agent` to grant the builder access to your SSH keys)
